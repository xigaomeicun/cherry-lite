# Deprecated upstream workflow neutralized for cherry-lite
name: ci-rerun-on-base-change
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "neutralized for cherry-lite"

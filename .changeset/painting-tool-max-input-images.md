---
---

Application-local change: the `generate_image` agent/chat tool now accepts up to the configured painting model's declared reference-image maximum (registry `maxInputImages`) instead of one. No package release is required.
